# element-plus-best

中后台前端最佳实践模板

## 克隆项目

克隆前需要关闭 git 的换行符自动转换，项目统一使用 LF

```bash
$ git config --global core.autocrlf false
```

## 安装依赖

```bash
$ pnpm i --frozen-lockfile
```

## 开发准备

1. 复制开发环境配置 `env/.env.development.eg` => `env/.env.development`

> 类型见: dts/env.d.ts
