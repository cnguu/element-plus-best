---
description: 测试与用例编写规范
alwaysApply: true
enabled: true
updatedAt: 2026-03-06T13:01:09.255Z
provider: 
---

# 测试规范（如项目中存在测试）

- **测试命名**
    - 测试文件以 `.spec.ts` 或 `.test.ts` 结尾，并与被测试模块保持相对位置或清晰可索引关系。
    - 使用描述性的 `describe` 与 `it` 文本，体现业务行为而不是实现细节。

- **测试内容**
    - 针对核心业务逻辑和工具函数编写单元测试，关注输入/输出与边界情况。
    - 避免对实现细节或易变内部结构进行过度耦合的测试。

- **可维护性**
    - 复用项目中已有的测试工具函数、测试配置与 helper，避免每个测试文件重复样板逻辑。
    - 对复杂场景可以通过工厂函数/构造函数创建测试数据，提高可读性。
